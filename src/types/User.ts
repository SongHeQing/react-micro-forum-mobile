export type LoginParams = {
  email: string;
  password: string;
}

export type LoginInfo = {
  id: number;
  nickname: string;
  email: string;
  token: string;
}

export type RegisterParams = {
  email: string;
  password: string;
}